Lint and format the codebase.

1. Run `rtk uv run ruff check . --fix` to auto-fix lint issues
2. Run `rtk uv run ruff format .` to format all files
3. Run `rtk uv run ruff check .` to show any remaining issues that need manual fixes
4. Report what was fixed and what remains
