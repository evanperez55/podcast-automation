"""Audio step: transcribe, analyze, censor, and normalize audio."""

from __future__ import annotations

from pipeline.context import PipelineContext


def run_audio(ctx: PipelineContext, components: dict) -> PipelineContext:
    """Transcribe, analyze, censor, and normalize episode audio.

    Stub — extraction pending Plan 02.
    """
    raise NotImplementedError("Extraction pending — Plan 02")
