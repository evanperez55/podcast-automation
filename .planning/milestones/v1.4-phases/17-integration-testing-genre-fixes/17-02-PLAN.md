---
phase: 17-integration-testing-genre-fixes
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - clients/true-crime-client.yaml
  - clients/business-interview-client.yaml
autonomous: false
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "True crime client YAML has episode_source: rss, rss_source with feed_url, clip_selection_mode: content, compliance_style: strict"
    - "Business interview client YAML has episode_source: rss, rss_source with feed_url, clip_selection_mode: content, compliance_style: standard"
    - "A real true crime episode processes end-to-end with genre-appropriate tone"
    - "A real business/interview episode processes end-to-end with genre-appropriate tone"
  artifacts:
    - path: "clients/true-crime-client.yaml"
      provides: "RSS source + genre content fields for true crime"
      contains: "clip_selection_mode: content"
    - path: "clients/business-interview-client.yaml"
      provides: "RSS source + genre content fields for business"
      contains: "compliance_style: standard"
  key_links:
    - from: "clients/true-crime-client.yaml"
      to: "client_config.py"
      via: "_YAML_TO_CONFIG maps clip_selection_mode and compliance_style"
      pattern: "clip_selection_mode.*content"
    - from: "pipeline/steps/ingest.py"
      to: "rss_episode_fetcher.py"
      via: "episode_source=rss triggers RSS ingest path"
      pattern: "episode_source.*rss"
---

<objective>
Update genre client YAMLs with RSS source and new content fields, then process real episodes to validate end-to-end genre-appropriate output.

Purpose: The code fixes from Plan 01 make clip selection and compliance genre-aware. This plan configures real RSS feeds in the genre YAMLs and runs actual episodes through the pipeline to empirically verify the output quality. This is the integration test that proves the pipeline works for non-comedy genres.

Output: Updated genre client YAMLs with RSS + content fields. Real episode output artifacts inspected for genre-appropriate tone.
</objective>

<execution_context>
@C:/Users/evanp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/evanp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-integration-testing-genre-fixes/17-RESEARCH.md
@.planning/phases/17-integration-testing-genre-fixes/17-01-SUMMARY.md

<interfaces>
<!-- Genre client YAML fields needed (from research + Plan 01 additions) -->

New YAML fields that Plan 01 added to _YAML_TO_CONFIG:
- content.clip_selection_mode -> CLIP_SELECTION_MODE (values: "energy", "content")
- content.compliance_style -> COMPLIANCE_STYLE (values: "permissive", "strict", "standard")

Existing YAML fields from Phase 16 (_YAML_TO_CONFIG):
- episode_source -> EPISODE_SOURCE (values: "dropbox", "rss", "local")
- rss_source.feed_url -> RSS_FEED_URL
- rss_source.episode_index -> RSS_EPISODE_INDEX

Public RSS feeds (MEDIUM confidence -- verify before use):
- Casefile True Crime: https://feeds.simplecast.com/xl3WWLBP
- How I Built This (NPR): https://feeds.simplecast.com/4T39_jAj
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update genre client YAMLs with RSS source and content fields</name>
  <files>clients/true-crime-client.yaml, clients/business-interview-client.yaml</files>
  <action>
Add the following fields to each genre client YAML. These fields enable RSS ingest (Phase 16) and genre-aware clip selection + compliance (Plan 01).

**true-crime-client.yaml** -- add these top-level and content fields:
```yaml
episode_source: "rss"

rss_source:
  feed_url: "https://feeds.simplecast.com/xl3WWLBP"  # Casefile True Crime
  episode_index: 0    # 0 = latest episode

# Under existing content: section, add:
  whisper_model: "small"          # Better accuracy for Australian accent
  clip_selection_mode: "content"  # Suppress RMS energy hints (flat audio)
  compliance_style: "strict"     # Conservative flagging for real crime content
```

**business-interview-client.yaml** -- add these top-level and content fields:
```yaml
episode_source: "rss"

rss_source:
  feed_url: "https://feeds.simplecast.com/4T39_jAj"  # How I Built This (NPR)
  episode_index: 0

# Under existing content: section, add:
  clip_selection_mode: "content"  # Favor substantive insight over energy peaks
  compliance_style: "standard"   # Standard YouTube guidelines
```

Place `episode_source` and `rss_source` at the top level (same indent as podcast_name). Place `whisper_model`, `clip_selection_mode`, and `compliance_style` inside the existing `content:` block alongside `num_clips`, `names_to_remove`, etc.

Also add "content.whisper_model": "WHISPER_MODEL" to _YAML_TO_CONFIG in client_config.py if not already present. Check first -- it may already exist from Phase 15/16. If it exists, skip this substep.

After editing, verify the YAML is valid:
```bash
uv run python -c "import yaml; yaml.safe_load(open('clients/true-crime-client.yaml'))"
uv run python -c "import yaml; yaml.safe_load(open('clients/business-interview-client.yaml'))"
```

Then run validate-client for each:
```bash
uv run main.py validate-client true-crime-client
uv run main.py validate-client business-interview-client
```

Both should print the active config showing the new fields (episode_source: rss, clip_selection_mode, compliance_style) without errors.
  </action>
  <verify>
    <automated>uv run python -c "import yaml; d=yaml.safe_load(open('clients/true-crime-client.yaml')); assert d['episode_source']=='rss'; assert d['content']['clip_selection_mode']=='content'; assert d['content']['compliance_style']=='strict'; print('true-crime OK')" && uv run python -c "import yaml; d=yaml.safe_load(open('clients/business-interview-client.yaml')); assert d['episode_source']=='rss'; assert d['content']['clip_selection_mode']=='content'; assert d['content']['compliance_style']=='standard'; print('business OK')"</automated>
  </verify>
  <done>Both genre YAMLs have episode_source: rss, rss_source with feed_url, clip_selection_mode: content, and genre-appropriate compliance_style. validate-client prints correct active config.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify real episode processing for both genres</name>
  <files>output/true-crime-client/, output/business-interview-client/</files>
  <action>
Process a real episode for each genre client. These require GPU (Whisper), network (RSS download + OpenAI API), and take 20-40 minutes each.

**Step 1 -- Process true crime episode:**
```bash
uv run main.py --client true-crime-client latest
```
If the latest episode is very long (90+ min), edit true-crime-client.yaml to set `episode_index: 1` for a shorter episode, then retry.

**Step 2 -- Inspect true crime output:**
After processing completes, check:
- Analysis JSON: open `output/true-crime-client/ep_*/..._analysis.json` and verify:
  - `episode_title` has no comedy framing (no "POV:", no irony)
  - `show_notes` lead with case facts, not entertainment language
  - `social_captions` are measured, not humorous
  - `best_clips[*].why_interesting` references case details/evidence, not "funny" or "entertaining"
  - No "fake problems" text anywhere
- Blog post: check for investigative tone, no comedy phrasing
- Compliance report: expect at least 1-2 flags for content discussing violence (strict mode). Zero flags on a case-heavy episode means compliance is still too permissive.

**Step 3 -- Process business interview episode:**
```bash
uv run main.py --client business-interview-client latest
```

**Step 4 -- Inspect business interview output:**
- Analysis JSON: verify:
  - `show_notes` lead with concrete data, not "In this episode..."
  - `best_clips[*].why_interesting` references specific insights or numbers
  - `social_captions` are professional, not casual
  - No "fake problems" text anywhere
- Blog post: check for professional, substantive tone
- Compliance report: expect 0 flags for mainstream business content (standard mode)

**Step 5 -- Regression check (optional but recommended):**
If Fake Problems episodes are available locally:
```bash
uv run main.py --dry-run latest
```
Verify dry-run completes without errors (backward compat).
  </action>
  <verify>Human inspects output artifacts for genre-appropriate tone and content quality.</verify>
  <done>Both genre episodes processed end-to-end. True crime output has investigative tone. Business output has professional tone. No Fake Problems contamination. Compliance flagging matches genre expectations.</done>
</task>

</tasks>

<verification>
1. Both genre YAMLs parse without error and contain all required fields
2. validate-client shows correct active config for each genre
3. Real true crime episode processes end-to-end with genre-appropriate output
4. Real business interview episode processes end-to-end with genre-appropriate output
5. No "fake problems" contamination in any non-FP client output
</verification>

<success_criteria>
- True crime episode: show notes, clips, and social captions have investigative/measured tone
- Business episode: show notes, clips, and social captions have professional/substantive tone
- Compliance checker flags appropriately per genre (strict for true crime, standard for business)
- Clip selection favors content quality over audio energy for both non-comedy genres
- No regression in Fake Problems default behavior
</success_criteria>

<output>
After completion, create `.planning/phases/17-integration-testing-genre-fixes/17-02-SUMMARY.md`
</output>
