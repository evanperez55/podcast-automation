---
phase: 05-architecture-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - pipeline/steps/ingest.py
  - pipeline/steps/audio.py
  - pipeline/steps/analysis.py
  - pipeline/steps/video.py
  - pipeline/steps/distribute.py
autonomous: true
requirements:
  - DEBT-01

must_haves:
  truths:
    - "Each step module contains the real implementation extracted from main.py process_episode()"
    - "All checkpoint key names are preserved exactly (transcribe, analyze, censor, normalize, create_clips, subtitles, convert_videos, convert_mp3, blog_post)"
    - "Step functions accept PipelineContext + components dict and return PipelineContext"
    - "No step module imports from main.py"
  artifacts:
    - path: "pipeline/steps/ingest.py"
      provides: "Step 1: download/find episode"
      contains: "def run_ingest"
      min_lines: 30
    - path: "pipeline/steps/audio.py"
      provides: "Steps 2, 4, 4.5, 6: transcribe, censor, normalize, mp3"
      contains: "def run_audio"
      min_lines: 80
    - path: "pipeline/steps/analysis.py"
      provides: "Steps 3, 3.5: content analysis, topic tracker"
      contains: "def run_analysis"
      min_lines: 30
    - path: "pipeline/steps/video.py"
      provides: "Steps 5, 5.1, 5.4, 5.5, 5.6: clips, approval, subs, video, thumbnail"
      contains: "def run_video"
      min_lines: 80
    - path: "pipeline/steps/distribute.py"
      provides: "Steps 7, 7.5, 8, 8.5, 9: dropbox, rss, social, blog, search"
      contains: "def run_distribute"
      min_lines: 80
  key_links:
    - from: "pipeline/steps/audio.py"
      to: "pipeline/context.py"
      via: "import and mutate PipelineContext"
      pattern: "from pipeline.context import PipelineContext"
    - from: "pipeline/steps/audio.py"
      to: "pipeline_state"
      via: "checkpoint complete_step calls"
      pattern: 'complete_step\("transcribe"'
    - from: "pipeline/steps/distribute.py"
      to: "pipeline_state"
      via: "checkpoint complete_step calls"
      pattern: 'complete_step\("blog_post"'
---

<objective>
Extract all pipeline step implementations from main.py's PodcastAutomation.process_episode() into the five pipeline/steps/ modules created in Plan 01.

Purpose: Move the actual processing logic out of the God Object. After this plan, all pipeline logic lives in pipeline/steps/ and the checkpoint key regression test goes GREEN.
Output: Five fully-implemented step modules; checkpoint key test passes.
</objective>

<execution_context>
@C:/Users/evanp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/evanp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-architecture-refactor/05-RESEARCH.md
@.planning/phases/05-architecture-refactor/05-01-SUMMARY.md

@main.py
@continue_episode.py
@pipeline_state.py
@config.py
</context>

<interfaces>
<!-- From Plan 01 — PipelineContext and step function signatures -->

```python
# pipeline/context.py — already created
from pipeline.context import PipelineContext

# Step function signature pattern:
def run_<group>(ctx: PipelineContext, components: dict, state=None) -> PipelineContext:
    """state is optional PipelineState for checkpoint resume support."""
```

<!-- Components dict keys (from main.py __init__): -->
```python
components = {
    "dropbox": DropboxHandler,
    "transcriber": Transcriber,
    "audio_processor": AudioProcessor,
    "editor": ContentEditor,
    "video_converter": VideoConverter | None,
    "uploaders": dict,  # {"youtube": ..., "twitter": ..., etc.}
    "topic_tracker": None,
    "notifier": DiscordNotifier,
    "scheduler": UploadScheduler,
    "blog_generator": BlogPostGenerator,
    "thumbnail_generator": ThumbnailGenerator,
    "clip_previewer": ClipPreviewer,
    "search_index": EpisodeSearchIndex,
    "audiogram_generator": AudiogramGenerator,
    "chapter_generator": ChapterGenerator,
}
```

<!-- Checkpoint keys (MUST preserve exactly): -->
"transcribe", "analyze", "censor", "normalize", "create_clips",
"subtitles", "convert_videos", "convert_mp3", "blog_post"

<!-- Step-to-line mapping from RESEARCH.md: -->
Step 1 (ingest): lines 830-856
Steps 2+4+4.5+6 (audio): lines 867-1203
Steps 3+3.5 (analysis): lines 887-941
Steps 5+5.1+5.4+5.5+5.6 (video): lines 976-1183
Steps 7+7.5+8+8.5+9 (distribute): lines 1204-1427
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract ingest, audio, and analysis step modules</name>
  <files>pipeline/steps/ingest.py, pipeline/steps/audio.py, pipeline/steps/analysis.py</files>
  <action>
Read main.py process_episode() carefully and extract logic into step modules. Each function takes `(ctx: PipelineContext, components: dict, state=None)` and returns ctx.

**pipeline/steps/ingest.py — run_ingest(ctx, components, state=None):**
Extract from main.py process_episode() lines ~830-856 (Step 1: download/find episode). This includes:
- Finding the episode folder via dropbox or local path
- Extracting episode_number from folder name
- Setting up episode_output_dir
- Set ctx.audio_file, ctx.episode_folder, ctx.episode_number, ctx.episode_output_dir
- Access components["dropbox"] for DropboxHandler
- Use Config for paths

**pipeline/steps/audio.py — run_audio(ctx, components, state=None):**
Extract Steps 2, 4, 4.5, 6 from process_episode():
- Step 2 (transcribe): ~lines 867-884. Uses components["transcriber"]. Sets ctx.transcript_data, ctx.transcript_path. Checkpoint key: "transcribe".
- Step 4 (censor): ~lines 942-958. Uses components["audio_processor"]. Sets ctx.censored_audio. Checkpoint key: "censor".
- Step 4.5 (normalize): ~lines 960-973. Uses components["audio_processor"]. Updates ctx.censored_audio. Checkpoint key: "normalize".
- Step 6 (convert to mp3): ~lines 1184-1203. Uses components["audio_processor"]. Sets ctx.mp3_path. Checkpoint key: "convert_mp3".
- Preserve ALL checkpoint resume logic (is_step_completed checks, get_step_outputs restores).
- Preserve the exact checkpoint key strings. Do NOT rename them.

**pipeline/steps/analysis.py — run_analysis(ctx, components, state=None):**
Extract Steps 3, 3.5 from process_episode():
- Step 3 (analyze): ~lines 887-922. Uses components["editor"]. Sets ctx.analysis. Checkpoint key: "analyze".
- Step 3.5 (topic tracker): ~lines 923-940. Uses components["topic_tracker"]. No checkpoint key.
- Read analysis from file if resume, use editor.analyze_content() otherwise.

CRITICAL: Copy the logic faithfully — do NOT refactor, simplify, or "improve" the extracted code. The goal is a mechanical move. Access `self.X` as `components["X"]` instead. Replace `self.test_mode` with `ctx.test_mode`, `self.resume` with `ctx.resume`, etc.

Add necessary imports at the top of each file: logging, Path, Config, etc. Each file must import PipelineContext from pipeline.context.
  </action>
  <verify>
    <automated>python -c "from pipeline.steps.ingest import run_ingest; from pipeline.steps.audio import run_audio; from pipeline.steps.analysis import run_analysis; print('Imports OK')" && pytest tests/test_pipeline_checkpoint_keys.py -v 2>&1 | tail -5</automated>
  </verify>
  <done>ingest.py, audio.py, analysis.py contain real extracted logic; checkpoint keys for transcribe, analyze, censor, normalize, convert_mp3 appear in source code</done>
</task>

<task type="auto">
  <name>Task 2: Extract video and distribute step modules</name>
  <files>pipeline/steps/video.py, pipeline/steps/distribute.py</files>
  <action>
Continue extracting from main.py process_episode():

**pipeline/steps/video.py — run_video(ctx, components, state=None):**
Extract Steps 5, 5.1, 5.4, 5.5, 5.6 from process_episode():
- Step 5 (create clips): ~lines 976-993. Uses components["audio_processor"]. Sets ctx.clip_paths. Checkpoint key: "create_clips".
- Step 5.1 (clip approval): Uses components["clip_previewer"]. Updates ctx.clip_paths. No checkpoint key.
- Step 5.4 (subtitles): Uses components["audio_processor"]. Sets ctx.srt_paths. Checkpoint key: "subtitles".
- Step 5.5 (convert videos): Uses components["video_converter"], components["audiogram_generator"]. Sets ctx.video_clip_paths, ctx.full_episode_video_path. Checkpoint key: "convert_videos". IMPORTANT: This step uses ThreadPoolExecutor — capture converter in closure variable before the `with` block: `vc = components["video_converter"]`.
- Step 5.6 (thumbnail): Uses components["thumbnail_generator"]. Sets ctx.thumbnail_path. No checkpoint key.
- Preserve ALL checkpoint resume logic exactly.

**pipeline/steps/distribute.py — run_distribute(ctx, components, state=None):**
Extract Steps 7, 7.5, 8, 8.5, 9 from process_episode():
- Step 7 (Dropbox upload): Uses components["dropbox"]. Sets ctx.finished_path.
- Step 7.5 (RSS feed): Uses RssFeedGenerator (import directly). Uses components["chapter_generator"].
- Step 8 (social media uploads): Uses components["uploaders"], components["scheduler"]. This includes the _upload_youtube, _upload_twitter, _upload_instagram, _upload_to_social_media helper logic. Sets ctx.uploaded_clip_paths. Extract the upload helper methods as module-level functions in distribute.py (prefixed with _).
- Step 8.5 (blog post): Uses components["blog_generator"]. Checkpoint key: "blog_post".
- Step 9 (search index): Uses components["search_index"].
- Also add `run_distribute_only(episode_number, skip_video=False, skip_upload=False)` function. This replaces continue_episode.py. Read continue_episode.py to understand its logic — it initializes only distribution components and runs Steps 7-9. The new function should: build a PipelineContext from existing files on disk for the given episode_number, init only distribution components, call run_distribute(ctx, components).

CRITICAL: Preserve checkpoint key strings exactly. Mechanical extraction — no refactoring.

Also extract the _upload_youtube, _upload_twitter, _upload_instagram helper methods from PodcastAutomation class into distribute.py as module-level functions. They take the same args but receive uploaders/scheduler from components dict.

After both tasks, run the checkpoint key test — it should now PASS since all 9 checkpoint keys appear in pipeline/steps/ source files.
  </action>
  <verify>
    <automated>pytest tests/test_pipeline_checkpoint_keys.py -v && python -c "from pipeline.steps.video import run_video; from pipeline.steps.distribute import run_distribute, run_distribute_only; print('All step imports OK')"</automated>
  </verify>
  <done>All 5 step modules contain real extracted logic; checkpoint key regression test PASSES; all 9 known keys found in pipeline/steps/ source; run_distribute_only exists in distribute.py</done>
</task>

</tasks>

<verification>
- `pytest tests/test_pipeline_checkpoint_keys.py -v` PASSES (all 9 keys found)
- All 5 step modules importable without errors
- `pytest` full suite still passes (no regressions — main.py unchanged, step modules are new files)
- No import from main.py in any pipeline/ file
</verification>

<success_criteria>
- Five step modules contain real implementation extracted from main.py
- All 9 checkpoint keys preserved exactly in step source files
- Checkpoint key regression test goes GREEN
- No changes to main.py yet (it still works as before)
- run_distribute_only() exists in distribute.py
</success_criteria>

<output>
After completion, create `.planning/phases/05-architecture-refactor/05-02-SUMMARY.md`
</output>
