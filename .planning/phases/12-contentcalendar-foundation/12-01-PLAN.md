---
phase: 12-contentcalendar-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content_calendar.py
  - tests/test_content_calendar.py
  - config.py
autonomous: true
requirements:
  - CAL-01
  - CAL-02

must_haves:
  truths:
    - "plan_episode() generates exactly 4 slots (teaser D-1, episode D0, clip_1 D+2, clip_2 D+4) when 2+ clips available"
    - "Clip slot count matches available video_clip_paths (0 clips = 0 clip slots, 1 clip = 1 slot, cap at 3)"
    - "No two slots share the same day_offset"
    - "plan_episode() is idempotent — second call for same episode returns existing entry"
    - "content_calendar.json is written atomically via .tmp + replace pattern"
    - "PostingTimeOptimizer provides slot hours with Config fallback when optimizer returns None"
  artifacts:
    - path: "content_calendar.py"
      provides: "ContentCalendar class with plan_episode, get_pending_slots, mark_slot_uploaded, load_all, save"
      exports: ["ContentCalendar"]
      min_lines: 120
    - path: "tests/test_content_calendar.py"
      provides: "Unit tests for all ContentCalendar methods"
      min_lines: 150
    - path: "config.py"
      provides: "CONTENT_CALENDAR_ENABLED flag and TOPIC_DATA_DIR constant"
      contains: "CONTENT_CALENDAR_ENABLED"
  key_links:
    - from: "content_calendar.py"
      to: "config.py"
      via: "Config.TOPIC_DATA_DIR, Config.CONTENT_CALENDAR_ENABLED, Config.SCHEDULE_*_POSTING_HOUR"
      pattern: "Config\\."
    - from: "content_calendar.py"
      to: "posting_time_optimizer.py"
      via: "PostingTimeOptimizer.get_optimal_publish_at()"
      pattern: "PostingTimeOptimizer"
    - from: "content_calendar.py"
      to: "topic_data/content_calendar.json"
      via: "atomic JSON write"
      pattern: "\\.replace\\("
---

<objective>
Build the ContentCalendar module that generates a per-episode 5-slot distribution plan spreading clips across D-1/D0/D+2/D+4, persists state to topic_data/content_calendar.json, and provides slot query/update methods.

Purpose: Replace same-day dump uploads with a deliberate weekly spread for better engagement.
Output: content_calendar.py module, comprehensive tests, config flag.
</objective>

<execution_context>
@C:/Users/evanp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/evanp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-contentcalendar-foundation/12-RESEARCH.md
@.planning/phases/12-contentcalendar-foundation/12-VALIDATION.md

@config.py
@scheduler.py
@posting_time_optimizer.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From config.py (lines 87-104):
```python
SCHEDULE_YOUTUBE_DELAY_HOURS = int(os.getenv("SCHEDULE_YOUTUBE_DELAY_HOURS", "0"))
SCHEDULE_TWITTER_DELAY_HOURS = int(os.getenv("SCHEDULE_TWITTER_DELAY_HOURS", "0"))
SCHEDULE_YOUTUBE_POSTING_HOUR = int(os.getenv("SCHEDULE_YOUTUBE_POSTING_HOUR", "14"))
SCHEDULE_TWITTER_POSTING_HOUR = int(os.getenv("SCHEDULE_TWITTER_POSTING_HOUR", "10"))
SCHEDULE_INSTAGRAM_POSTING_HOUR = int(os.getenv("SCHEDULE_INSTAGRAM_POSTING_HOUR", "12"))
SCHEDULE_TIKTOK_POSTING_HOUR = int(os.getenv("SCHEDULE_TIKTOK_POSTING_HOUR", "12"))
```

From scheduler.py (atomic write pattern, lines 129-133):
```python
tmp_path = output_dir / "upload_schedule.json.tmp"
tmp_path.write_text(json.dumps(schedule, indent=2), encoding="utf-8")
tmp_path.replace(schedule_path)
```

From posting_time_optimizer.py:
```python
class PostingTimeOptimizer:
    def get_optimal_publish_at(self, platform: str) -> Optional[datetime]:
        # Returns None when insufficient data
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ContentCalendar module with tests</name>
  <files>content_calendar.py, tests/test_content_calendar.py, config.py</files>
  <behavior>
    - plan_episode(episode_number=29, release_date=2026-03-19, analysis={best_clips: [{hook_caption: "..."}]}, video_clip_paths=["a.mp4","b.mp4"]) produces 4 slots: teaser (D-1, twitter), episode (D0, youtube+twitter), clip_1 (D+2, youtube+twitter), clip_2 (D+4, youtube+twitter)
    - plan_episode with 0 video_clip_paths produces 2 slots (teaser + episode only); with 1 clip produces 3 slots; cap at 3 clip slots
    - plan_episode with no best_clips in analysis skips the teaser slot entirely
    - No two slots have the same day_offset value
    - plan_episode called twice for same episode returns existing entry without overwriting (idempotent)
    - Slot scheduled_at uses PostingTimeOptimizer hour when available, falls back to Config.SCHEDULE_*_POSTING_HOUR
    - get_pending_slots returns only slots where status=="pending" AND scheduled_at <= now
    - get_pending_slots skips slots with status "uploaded" or "failed"
    - mark_slot_uploaded sets status="uploaded", uploaded_at=now, stores upload_results
    - mark_slot_failed sets status="failed" with error message
    - save() writes atomically via .tmp + Path.replace()
    - load_all() returns empty dict when file doesn't exist
    - ContentCalendar respects self.enabled from Config.CONTENT_CALENDAR_ENABLED
  </behavior>
  <action>
    **config.py additions** (2 lines in Config class, after line ~104):
    - Add `CONTENT_CALENDAR_ENABLED = os.getenv("CONTENT_CALENDAR_ENABLED", "true") == "true"`
    - Add `TOPIC_DATA_DIR = Path("topic_data")`

    **content_calendar.py** — new top-level module:
    - Import: `json, dataclasses, pathlib.Path, datetime, timedelta, typing, config.Config, posting_time_optimizer.PostingTimeOptimizer, logger`
    - Class `ContentCalendar` with `self.enabled = Config.CONTENT_CALENDAR_ENABLED` and `self.calendar_path = Config.TOPIC_DATA_DIR / "content_calendar.json"`
    - Method `plan_episode(episode_number, release_date, analysis, video_clip_paths=None, full_episode_video_path=None)`:
      - Build episode key `f"ep_{episode_number}"`
      - Load existing data; if episode key exists, return existing entry (idempotent guard)
      - Build slots dict: always add "episode" slot (D0, platforms=["youtube","twitter"])
      - If analysis has best_clips[0] with hook_caption, add "teaser" slot (D-1, platforms=["twitter"])
      - For each video_clip_path (cap at 3): add "clip_N" slot at D+2, D+4, D+6 offsets (ensuring clip_1=D+2, clip_2=D+4, clip_3=D+6)
      - For each slot, compute scheduled_at via `_slot_datetime(release_date, day_offset, primary_platform)`
      - Each slot: `{slot_type, day_offset, scheduled_at (ISO string), platforms, clip_index (or null), content: {caption/clip_path/title/video_path}, status: "pending", uploaded_at: null, upload_results: {}}`
      - Save and return the episode entry
    - Helper `_slot_datetime(release_date, day_offset, platform)`: try PostingTimeOptimizer.get_optimal_publish_at(platform), if None use Config.SCHEDULE_{PLATFORM}_POSTING_HOUR, compute base = release_date + timedelta(days=day_offset), replace hour
    - Method `get_pending_slots(episode_data)`: iterate slots, return list of slots where status=="pending" and scheduled_at <= datetime.now()
    - Method `mark_slot_uploaded(episode_key, slot_name, upload_results)`: load, set status="uploaded", uploaded_at=now.isoformat(), upload_results=results, save
    - Method `mark_slot_failed(episode_key, slot_name, error)`: load, set status="failed", error=error, save
    - Method `load_all()`: read calendar_path JSON, return dict (empty dict if file missing)
    - Method `save(data)`: atomic write via .tmp + Path.replace() following scheduler.py pattern
    - Method `get_calendar_display(episode_number, release_date)`: returns list of slot dicts with label/dt/type/platforms for dry-run display (does NOT persist)

    **tests/test_content_calendar.py** — write tests FIRST (TDD):
    - class TestPlanEpisode: test_generates_correct_slots_with_2_clips, test_no_clips_skips_clip_slots, test_one_clip_one_slot, test_cap_at_3_clips, test_no_best_clips_skips_teaser, test_no_duplicate_day_offsets, test_idempotent
    - class TestSaveLoad: test_atomic_write, test_load_missing_file_returns_empty, test_round_trip
    - class TestGetPendingSlots: test_returns_past_due_pending, test_skips_uploaded, test_skips_future
    - class TestMarkSlot: test_mark_uploaded, test_mark_failed
    - class TestSlotDatetime: test_uses_optimizer_hour, test_falls_back_to_config_hour
    - Mock PostingTimeOptimizer and Config as needed. Use tmp_path fixture for calendar file. Patch Config.TOPIC_DATA_DIR to tmp_path.
  </action>
  <verify>
    <automated>rtk pytest tests/test_content_calendar.py -x -v</automated>
  </verify>
  <done>
    - content_calendar.py exists with ContentCalendar class exporting plan_episode, get_pending_slots, mark_slot_uploaded, mark_slot_failed, load_all, save, get_calendar_display
    - config.py has CONTENT_CALENDAR_ENABLED and TOPIC_DATA_DIR
    - All tests in tests/test_content_calendar.py pass
    - plan_episode generates correct slot layout per CAL-01
    - Atomic write pattern matches scheduler.py convention per CAL-02
    - Full test suite (`pytest`) still passes
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_content_calendar.py -x -v` — all ContentCalendar unit tests green
- `pytest` — full suite green (no regressions)
- `ruff check content_calendar.py tests/test_content_calendar.py` — no lint issues
</verification>

<success_criteria>
- ContentCalendar.plan_episode() generates 5-slot plans (D-1 teaser, D0 episode, D+2/D+4/D+6 clips) with correct day offsets
- Calendar state persists to topic_data/content_calendar.json via atomic writes
- Slot status tracking works (pending -> uploaded/failed)
- Module follows project conventions (self.enabled pattern, flat structure, unittest.mock in tests)
</success_criteria>

<output>
After completion, create `.planning/phases/12-contentcalendar-foundation/12-01-SUMMARY.md`
</output>
