---
phase: 23-monitoring-alerting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/runner.py
  - notifications.py
  - tests/test_notifications.py
  - tests/test_runner.py
autonomous: true
requirements:
  - MON-01
  - MON-02

must_haves:
  truths:
    - "When any pipeline step raises an unhandled exception, a Discord message is sent with the episode name, step name, and error text before the process exits"
    - "When a pipeline run completes successfully, a Discord summary message shows episode name, total duration, number of clips produced, and which platforms received uploads"
    - "When DISCORD_WEBHOOK_URL is not set, both alert behaviors disable silently without affecting pipeline execution"
    - "Multi-client runs each send their own notification per client"
  artifacts:
    - path: "pipeline/runner.py"
      provides: "Per-step error capture and duration tracking"
      contains: "time.time"
    - path: "notifications.py"
      provides: "Enhanced notify_success with duration field"
      contains: "Duration"
    - path: "tests/test_notifications.py"
      provides: "Tests for enhanced notify_success with duration"
      contains: "test_notify_success_with_duration"
    - path: "tests/test_runner.py"
      provides: "Tests for per-step notification on failure"
      contains: "test_step_failure_sends_notification"
  key_links:
    - from: "pipeline/runner.py"
      to: "notifications.py"
      via: "DiscordNotifier().notify_failure(episode_info, error, step=step_name)"
      pattern: "notify_failure.*step="
    - from: "pipeline/runner.py"
      to: "notifications.py"
      via: "DiscordNotifier().notify_success(results) with duration in results"
      pattern: "notify_success"
---

<objective>
Wire Discord notifications into the pipeline so every failure and success is visible without checking logs.

Purpose: Paying clients need operational visibility -- no silent failures, no guessing if a run succeeded.
Output: Pipeline sends Discord alerts on per-step failures and success summaries with duration/clip/platform info.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pipeline/runner.py
@notifications.py
@tests/test_notifications.py
@config.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From notifications.py:
```python
class DiscordNotifier:
    def __init__(self, webhook_url=None):
        self.webhook_url = webhook_url or Config.DISCORD_WEBHOOK_URL
        self.enabled = bool(self.webhook_url)

    def send_notification(self, title, description, color=0x00FF00, fields=None) -> bool: ...
    def notify_success(self, results: dict) -> bool: ...
    def notify_failure(self, episode_info, error, step="unknown") -> bool: ...
    def notify_partial_success(self, results, warnings) -> bool: ...
```

From pipeline/runner.py (key functions):
```python
def run_with_notification(args, episode_number=None, dropbox_path=None, local_audio_path=None):
    # Currently wraps run() with try/except, calls notify_success/notify_failure
    # Only catches at step="process_episode" level -- no per-step granularity

def _run_pipeline(args):
    # Calls: run_ingest -> _run_transcribe -> run_analysis -> _run_process_audio -> run_video -> run_distribute
    # No per-step try/except -- exceptions bubble up to run_with_notification
```

From config.py:
```python
class Config:
    DISCORD_WEBHOOK_URL = os.getenv("DISCORD_WEBHOOK_URL")  # line 145
```

From pipeline/runner.py results dict (lines 556-580):
```python
results = {
    "episode_number": episode_number,
    "episode_title": episode_title,
    "clips": [str(p) for p in ctx.clip_paths],
    "social_media_results": ...,  # set during distribute step
    # NOTE: no "duration" key currently
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add per-step failure notifications and duration tracking to pipeline runner</name>
  <files>pipeline/runner.py, notifications.py</files>
  <read_first>
    - pipeline/runner.py (full file -- understand current step flow and run_with_notification)
    - notifications.py (full file -- see current notify_success/notify_failure signatures)
    - config.py (line 145 -- DISCORD_WEBHOOK_URL)
  </read_first>
  <behavior>
    - Test: notify_success includes "Duration" field when results dict has "duration_seconds" key
    - Test: notify_success formats duration as "Xm Ys" (e.g., "12m 34s")
    - Test: notify_success still works when "duration_seconds" is absent (backward compat)
  </behavior>
  <action>
**notifications.py changes:**

In `notify_success`, add a "Duration" field to the fields list. Read `results.get("duration_seconds")` and format as `"{m}m {s}s"`. Insert the field after the "Episode #" field. If `duration_seconds` is not present in results, omit the Duration field (backward compatibility).

Concrete change -- after the existing `"Episode #"` field dict in notify_success's `fields` list, add:

```python
duration_secs = results.get("duration_seconds")
if duration_secs is not None:
    minutes = int(duration_secs // 60)
    seconds = int(duration_secs % 60)
    fields.append({
        "name": "Duration",
        "value": f"{minutes}m {seconds}s",
        "inline": True,
    })
```

Move the `fields = [...]` to be built incrementally so the conditional Duration field can be inserted.

**pipeline/runner.py changes:**

1. In `_run_pipeline` (line ~362), add `import time` at top of function and record `start_time = time.time()` right before the first step call (line ~461, before `ctx = run_ingest(...)`).

2. Wrap each pipeline step call in `_run_pipeline` with a helper. Add a local function `_run_step(step_name, fn, *args)` inside `_run_pipeline` that:
   - Calls `fn(*args)`
   - On exception: gets the notifier from `components["notifier"]`, calls `notifier.notify_failure(episode_info, e, step=step_name)` where `episode_info` is `ctx.episode_number or ctx.episode_folder or "unknown"`, then re-raises
   - Returns the function result on success

3. Replace each bare step call with the wrapped version:
   - `ctx = run_ingest(ctx, components, state)` becomes the call inside `_run_step("ingest", run_ingest, ctx, components, state)` and assigns result to ctx
   - Same for `_run_transcribe` (step="transcribe"), `run_analysis` (step="analysis"), `_run_process_audio` (step="audio_processing"), `run_video` (step="video"), `run_distribute` (step="distribute")
   - The compliance check block (lines 500-522) should also be wrapped: step="compliance_check"

4. After the results dict is built (line ~580), compute `elapsed = time.time() - start_time` and add `results["duration_seconds"] = elapsed`.

5. In `run_with_notification` (line ~1068): since per-step notifications now happen inside `_run_pipeline`, the outer try/except still serves as a safety net. Keep it but update `step="process_episode"` to `step="pipeline_setup"` to distinguish from per-step errors. This way, errors during arg parsing or component init (before steps begin) get their own notification.

**Important:** The notifier is already in `components["notifier"]` (line 186). The `_run_step` helper accesses it from the components dict. If notifier is None or not enabled, `notify_failure` returns False silently -- no crash.
  </action>
  <verify>
    <automated>cd C:/Users/evanp/projects/podcast-automation && uv run pytest tests/test_notifications.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - notifications.py contains `results.get("duration_seconds")`
    - notifications.py contains `"Duration"` as a field name string
    - notifications.py contains `f"{minutes}m {seconds}s"` or equivalent format string
    - pipeline/runner.py contains `import time` or `from time import time`
    - pipeline/runner.py contains `start_time = time.time()` or `start_time = time()`
    - pipeline/runner.py contains `results["duration_seconds"]`
    - pipeline/runner.py contains `notify_failure` call with `step=` kwarg referencing step names like "ingest", "transcribe", "analysis"
    - pipeline/runner.py contains a step-wrapping pattern (function or try/except per step) that catches Exception and calls notify_failure before re-raising
    - `uv run pytest tests/test_notifications.py -x` exits 0
  </acceptance_criteria>
  <done>
    - notify_success includes Duration field formatted as "Xm Ys" when duration_seconds present in results
    - notify_success omits Duration field when duration_seconds absent (backward compat)
    - Each pipeline step (ingest, transcribe, analysis, audio_processing, compliance, video, distribute) is wrapped with per-step error notification
    - Pipeline tracks total duration and includes it in the results dict
    - All existing notification tests still pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests for per-step failure notifications and duration tracking</name>
  <files>tests/test_runner.py, tests/test_notifications.py</files>
  <read_first>
    - tests/test_notifications.py (full file -- see existing test patterns and class structure)
    - tests/test_runner.py (if exists -- check existing runner tests)
    - pipeline/runner.py (lines 362-610 -- the _run_pipeline function after Task 1 changes)
    - notifications.py (after Task 1 changes -- verify new Duration field)
  </read_first>
  <behavior>
    - Test: notify_success with duration_seconds=754 produces field "Duration" = "12m 34s"
    - Test: notify_success without duration_seconds key produces no Duration field
    - Test: _run_pipeline step failure calls notify_failure with correct step name
    - Test: _run_pipeline step failure re-raises the original exception after notifying
    - Test: _run_pipeline success path includes duration_seconds in results dict
    - Test: When notifier.enabled is False, step failure does not attempt Discord POST
  </behavior>
  <action>
**tests/test_notifications.py additions:**

Add to the existing `TestDiscordNotifier` class (or create a new `TestNotifySuccessDuration` class):

1. `test_notify_success_with_duration` -- pass results dict with `"duration_seconds": 754`. Assert the embed fields contain a field with `"name": "Duration"` and `"value": "12m 34s"`.

2. `test_notify_success_without_duration` -- pass results dict WITHOUT `duration_seconds` key. Assert no field in the embed has `"name": "Duration"`.

3. `test_notify_success_zero_duration` -- pass `"duration_seconds": 0`. Assert Duration field shows "0m 0s".

All tests follow the existing pattern: `@patch("notifications.requests.post")`, create `DiscordNotifier(webhook_url="https://discord.com/api/webhooks/test")`, call method, inspect `mock_post.call_args.kwargs["json"]["embeds"][0]["fields"]`.

**tests/test_runner.py additions:**

If the file exists, add to it. If not, create it following the project test conventions.

1. `test_step_failure_sends_notification` -- Mock `run_ingest` to raise `Exception("Download failed")`. Mock `DiscordNotifier.notify_failure`. Call `_run_pipeline` with minimal args dict. Assert `notify_failure` was called with `step="ingest"` and the error message contains "Download failed". Assert the exception is re-raised (use `pytest.raises`).

2. `test_pipeline_success_includes_duration` -- Mock all pipeline steps to return a minimal ctx. Mock `time.time` to return controlled values (e.g., first call returns 1000.0, subsequent calls return 1000.0, final call returns 1754.0). Assert returned results dict contains `"duration_seconds"` and value is approximately 754.

3. `test_step_failure_notifier_disabled_no_crash` -- Same as test 1 but with `DiscordNotifier(webhook_url=None)`. Assert the exception still re-raises without errors from the notification attempt.

Use `@patch` for all external dependencies (DiscordNotifier, step functions, Config attributes). Use `patch.object(Config, "OUTPUT_DIR", tmp_path)` and `patch.object(Config, "DISCORD_WEBHOOK_URL", None)` as needed. Follow the project's `unittest.mock` conventions.
  </action>
  <verify>
    <automated>cd C:/Users/evanp/projects/podcast-automation && uv run pytest tests/test_notifications.py tests/test_runner.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_notifications.py contains `test_notify_success_with_duration`
    - tests/test_notifications.py contains `test_notify_success_without_duration`
    - tests/test_notifications.py contains `"12m 34s"` (expected duration format)
    - tests/test_runner.py contains `test_step_failure_sends_notification`
    - tests/test_runner.py contains `test_pipeline_success_includes_duration`
    - tests/test_runner.py contains `step="ingest"` or similar step name assertion
    - `uv run pytest tests/test_notifications.py tests/test_runner.py -x` exits 0
  </acceptance_criteria>
  <done>
    - 3+ new tests in test_notifications.py covering duration field presence/absence/format
    - 3+ new tests in test_runner.py covering per-step failure notification, duration tracking, and disabled notifier
    - All tests pass including existing tests
    - Full test suite passes: `uv run pytest` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline -> Discord API | Outbound webhook POST with pipeline data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | notifications.py notify_failure | mitigate | Error text sent to Discord may contain file paths or config values. Truncate error text to 500 chars in notify_failure to limit exposure. Do not include env var values or credentials in error messages. |
| T-23-02 | Denial of Service | pipeline/runner.py | accept | Discord webhook rate limits (30 req/60s) could be hit during multi-client batch runs with failures. Acceptable: notifications are fire-and-forget with existing 10s timeout; pipeline continues regardless. |
| T-23-03 | Spoofing | Discord webhook URL | accept | Webhook URL stored in env var, not in code. If compromised, attacker receives pipeline status but cannot modify pipeline. Low value target. |
</threat_model>

<verification>
1. Run `uv run pytest tests/test_notifications.py tests/test_runner.py -x -v` -- all pass
2. Run `uv run pytest -x` -- full suite passes (no regressions)
3. Run `uv run ruff check notifications.py pipeline/runner.py` -- no lint errors
4. Grep `pipeline/runner.py` for `notify_failure.*step=` -- at least 5 matches (one per step)
5. Grep `notifications.py` for `duration_seconds` -- at least 1 match
6. Grep `pipeline/runner.py` for `duration_seconds` -- at least 1 match
</verification>

<success_criteria>
- MON-01: Every pipeline step (ingest, transcribe, analysis, audio_processing, compliance, video, distribute) sends a Discord failure alert with step name and error text when it raises an exception
- MON-02: Successful pipeline runs send a Discord summary with episode name, formatted duration, clip count, and platform list
- Silent disable: When DISCORD_WEBHOOK_URL is unset, no notification code crashes and pipeline executes normally
- Backward compat: Existing notify_success calls without duration_seconds still work
- All tests pass: `uv run pytest -x` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/23-monitoring-alerting/23-01-SUMMARY.md`
</output>
